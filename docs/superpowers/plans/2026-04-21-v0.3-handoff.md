# v0.3 Alignment — Copilot Handoff Brief

**交付对象**:Copilot
**验收人**:bbing(2026-04-22 起)
**产出**:v0.3-alignment 分支提交,不合 main

## 环境

- Worktree:`/Users/bing/-Code-/qwen-plugin-cc-v0.3`
- 分支:`v0.3-alignment`(off main `5611a43`)
- qwen CLI:本机已认证(v0.14.5,Alibaba Cloud Coding Plan)
- Node:20+,零 npm 依赖
- 测试基线:224 tests 全绿

## 任务

按 `docs/superpowers/plans/2026-04-21-v0.3-alignment.md` 顺序执行:

1. **Task 0.1** — Phase 0 probe(case-14,探 qwen CLI result event schema)→ 写 F-18 到 `doc/probe/FINDINGS.md`
2. **Task 1.1** — 加 `/qwen:ask` 命令(与 probe 可并行)
3. **Task 2.0** — 读 F-18 决策结果,**amend 本 plan** 展开 Task 2.1+(三选一:接通六段 / 接通三段 / 删 timing)
4. **Task 2.1+** — 按 amend 后的 plan 执行

每 task 按 TDD:写测试 → 测试失败 → 实现 → 测试通过 → commit。不累积 commit。

## 约束

- 不合 main,不打 tag(验收后 bbing 操作)
- 不碰 v0.2.1 hotfix 已解决项(见 `CLAUDE.md` "v0.2.1 hotfix 约束" 不要重报)
- FINDINGS 权威 > spec
- 每 commit 跑 `node --test plugins/qwen/scripts/tests/*.test.mjs`,0 回归
- 日志/状态持久化到 `tasks/progress-v0.3.md`(便于验收时追)

## 验收检查表

```bash
cd /Users/bing/-Code-/qwen-plugin-cc-v0.3

# 1. 分支状态
git log --oneline main..HEAD

# 2. 测试不退化
node --test plugins/qwen/scripts/tests/*.test.mjs 2>&1 | tail -3

# 3. probe 产出
cat doc/probe/case-14-result-event.result.json | jq .scalar_key_paths
grep -A 8 "^### F-18" doc/probe/FINDINGS.md

# 4. ask 命令存在
ls plugins/qwen/commands/ask.md

# 5. plan 已 amend(Task 2.1+ 占位段应消失)
grep -c "待 Task 2.0 amend 后填入" docs/superpowers/plans/2026-04-21-v0.3-alignment.md  # 应 = 0

# 6. 进度日志
cat tasks/progress-v0.3.md
```

## 参考

- 对齐报告:`/Users/bing/-Code-/gemini-plugin-cc/docs/alignment/qwen.md`(gemini 视角,同机)
- gemini baseline:`/Users/bing/-Code-/gemini-plugin-cc/docs/alignment/baseline.md`
- FINDINGS F-1..F-17:`doc/probe/FINDINGS.md`(17 条 qwen 实测行为)
- 既往 review 归档:`doc/review-v02-*.md`(6-way,v0.2.0 修复参考)

## 不做

- 不恢复 gemini v0.5.2 血统的 timing 基础设施(除非 Task 2.0 决策 = 接通)
- 不改 job-control.mjs / job-lifecycle.mjs 拆分
- 不重写 hook 脚本
- 不加 npm 依赖
